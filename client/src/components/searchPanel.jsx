import React from "react";

const SearchPanel = () => {
    return (
        <div>
            <input type="text" name="searchQuery" placeholder="Поиск..." />
        </div>
    );
};

export default SearchPanel;
